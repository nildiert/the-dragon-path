<div class="col-12">


<% if current_user %>
    <div>
<!--      <h2><%#= current_user.profile.full_name %></h2>-->

    </div>

  <div class="mt-4">

    <h4>Cursos</h4>
    <% @courses.each do |course_per_user| %>
      <ul class="list-group">
          <li class="list-group-item text-primary"><%= link_to course_per_user.course.title, course_per_user %></li>
        </ul>
    <% end %>
  </div>


  <div class="mt-4">
    <%= link_to "Sign out", destroy_user_session_path, 'data-turbo-method': :delete, :class => "btn btn-primary"  %>

  </div>


<% else  %>
  <p>You are not signed in.</p>
  <%= link_to "Sign Up!", new_user_registration_path  %>
  <br>
  <%= link_to "Login!", new_user_session_path %>

  <%= %>>
video

<% end %>
</div>
