<%= form_with(model: course_per_user) do |form| %>
  <% if course_per_user.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(course_per_user.errors.count, "error") %> prohibited this course_per_user from being saved:</h2>

      <ul>
        <% course_per_user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :course_id, style: "display: block" %>
    <%= form.text_field :course_id %>
  </div>

  <div>
    <%= form.label :status, style: "display: block" %>
    <%= form.number_field :status %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
