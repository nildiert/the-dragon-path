<%= form_with(model: video_per_user) do |form| %>
  <% if video_per_user.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(video_per_user.errors.count, "error") %> prohibited this video_per_user from being saved:</h2>

      <ul>
        <% video_per_user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :video_id, style: "display: block" %>
    <%= form.text_field :video_id %>
  </div>

  <div>
    <%= form.label :status, style: "display: block" %>
    <%= form.number_field :status %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
